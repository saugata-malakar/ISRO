"""Operational entry-point scripts (seed, train, index, demo, model download).

These are the documented one-command tools the Makefile shells out to. The
heavy lifting lives in src/neuralink; these orchestrate it.
"""
